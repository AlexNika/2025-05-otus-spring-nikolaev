spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  shell:
    interactive:
      enabled: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0